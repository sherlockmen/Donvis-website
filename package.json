{
  "name": "donvis-homepage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "typescript": "^5.8.3",
    "vite": "^6.4.3",
    "vue": "^3.4.38",
    "vue-tsc": "^3.3.5"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.41.0",
    "wrangler": "^4.101.0"
  }
}
